name: Workflow Test

on: push

jobs:
  prim_job:
    runs-on: ubuntu-latest
    steps:
      - name: Check run
        run: echo "success"

      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: List files
        run: ls

      - name: Read README
        run: |
          cat README.md
